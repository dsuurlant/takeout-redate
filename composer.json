{
    "name": "takeout-redate/cli",
    "description": "A Symfony Console CLI tool",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": "^8.4",
        "symfony/console": "^6.0|^7.0",
        "symfony/process": "^6.0|^7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0",
        "humbug/box": "^4.0",
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpstan/phpstan": "^1.10"
    },
    "autoload": {
        "psr-4": {
            "TakeoutRedate\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "TakeoutRedate\\Tests\\": "tests/"
        }
    },
    "bin": [
        "bin/app"
    ],
    "scripts": {
        "test": "phpunit",
        "build": "box compile --no-parallel --allow-composer-check-failure",
        "test-build": [
            "@test",
            "@build",
            "@test-build:verify"
        ],
        "test-build:verify": "php -r \"if (!file_exists('takeout-redate.phar')) { echo 'PHAR file not found'; exit(1); }\" && php takeout-redate.phar list",
        "cs-fix": "php-cs-fixer fix",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "phpstan": "phpstan analyse",
        "quality": [
            "@cs-fix",
            "@phpstan"
        ]
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "php-http/discovery": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
